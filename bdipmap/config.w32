// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bdipmap", "for bdipmap support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bdipmap", "enable bdipmap support", "no");

if (PHP_BDIPMAP != "no") {
	EXTENSION("bdipmap", "bdipmap.c");
}

